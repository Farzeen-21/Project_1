<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core" xmlns:f="sap.ui.layout.form" 
    xmlns:card="sap.f.cards" xmlns:form="sap.ui.layout.form">
	<!-- <Popover placement="Bottom" showHeader="false" contentWidth="250px"> -->
    <Popover placement="Bottom" showHeader="false" >
        <!-- <VBox class="sapUiTinyMargin"> -->
            <f:SimpleForm id="simpleForm12" editable="true" layout="ResponsiveGridLayout" singleContainerFullSize="false">
                <f:content>
                    <Label text="Designation" />
					<Text text="{formModel>/DESIGNATION}"/>

                    <Label text="GL Balance" />
					<Text text="{formModel>/GL_LeaveBalance}"/>

                    <Label text="CL Balance"/>
                    <Text text="{formModel>/CL_LeaveBalance}" />

                    <Label text="Projects"/>
                    <Text text="{formModel>/PROJECTS}" />
                </f:content>
            </f:SimpleForm>
            <!-- <HBox>
                <Label text="GL Balance : "></Label>
                <Text text="  {formModel>/GL_LeaveBalance}" class="sapUiTinyMarginBegin" />
            </HBox>
            <HBox>
                <Label text="CL Balance : "></Label>
                <Text text="  {formModel>/CL_LeaveBalance}" class="sapUiTinyMarginBegin" />
            </HBox>
            <HBox>
                <Label text="Projects : "></Label>
                <Text text="  {formModel>/PROJECTS}" class="sapUiTinyMarginBegin" />
            </HBox> -->
        <!-- </VBox> -->
	</Popover>
</core:FragmentDefinition>